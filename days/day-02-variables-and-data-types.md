# Day 2: Understanding Variables and Data Types

Date: 2026-07-15
Topic: Variable declaration, naming conventions, the `print()` function, and type-checking with `type()` and `isinstance()`.

## What I did
Worked through the "Understanding Variables and Data Types" module.

## What clicked
- Python's core built-in data types
- How to declare variables and Python's variable naming conventions
- How the `print()` function works
- How to check a variable's data type using `type()` and `isinstance()`

## What was confusing
The lesson introduced `isinstance()` for type-checking, but demonstrated it without wrapping the call in `print()` — unlike `type()`, which was taught with `print()` from the start. Running `isinstance()` alone produced no visible output, which was confusing until I wrapped it in `print()` and could see the boolean result.

## Tomorrow
First mini project — Build a Report Card Printer

<img width="950" height="976" alt="Screenshot 2026-07-17 at 3 09 11 PM" src="https://github.com/user-attachments/assets/2f34dea4-cdf6-4f30-a7cf-22bf48b73fa7" />
